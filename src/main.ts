import { createApp } from "vue";
import App from "./App.vue";
import "./styles/main.css";

createApp(App, {
  pluginName: "Onin Dev Toolbox",
  pluginId: "com.example.onin-dev-toolbox",
}).mount("#app");
